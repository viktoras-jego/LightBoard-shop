{% extends 'base.html.twig' %}

{% block body %}
    <head>
        <div style="position: relative; width: 2000px; top: 0px; background-image:url('{{ asset('images/abc2.png') }}')"></div>

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">


        <link rel="stylesheet" type="text/css" href="{{ asset('css/bootstrap.css') }}">

    </head>

    <div>
        <div style="position: relative; height: 935px; top: 0px; background-image:url('{{ asset('images/abc2.png') }}')">
            <div style="position: relative; width: 2000px; top: 0px; background-image:url('{{ asset('images/abc2.png') }}')">
        <br>
        <br>

        <nav class="navbar navbar-fixed-top">

            <div class="container-fluid">
                <div style="position: absolute; top: 0; right: 0; width: 650px; text-align:left;">
                    <br>
                    <button class="button_test">SHOP </button>
                    &nbsp
                    <button class="button_test">ABOUT US </button>
                    &nbsp
                    <button class="button_test">SUPPORT </button>
                </div>


            </div>
        </nav>


    </div>
    {% for flash_message in app.session.flashBag.get('notice') %}
        <div class="flash-notice">

            <div class="alert alert-danger fade in">
                <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                <center> <strong>Not enough money</strong></center>
            </div>

        </div>
    {% endfor %}


    <div class="hero-unit">
        <h1>
            Pinigai:


                {{kaina}}

            <a href="{{ path("add_produktas") }}"> +</a>
        </h1>
    </div>
    <div id="wrapper">

        <div id="container">
            <h3>   <a href="{{ path("add_car") }}">Add car </a></h3>
<table class="table">

    <thead>
    <tr>
    <th>Title</th>
    <th>Price</th>
    <th>Action</th>
    </tr>
    </thead>
    <h3>
        <tbody>
        {% for car in cars %}
            <tr>
                   <td> {{ car.comment }}</td>
                    <td>{{ car.Price }}</td>
                <td>
                    <a href ="{{ path("edit_car",{car: car.id}) }}">
                        EDIT
                    </a>
                    |
                    <a href="{{ path("delete_car",{car: car.id}) }}">
                        BUY
                    </a>
                </td>


            </tr>
        {% endfor %}
        </tbody>
    </h3>
</table>




        </div>

    </div>


<script>


    setTimeout(function () {
        $( ".flash-notice" ).fadeOut();
    }, 2350)

</script>
        </div>
{% endblock %}

{% block stylesheets %}
    <style>
       body { background: #F5F5F5; font: 20px/1.5 sans-serif; }
       a {text-decoration: none}
       h1, h2 { line-height: 1.2; margin: 0 0 .0em; }
        h1 { font-size: 20px; }
        h2 { font-size: 21px; margin-bottom: 1em; }

        p { margin: 0 0 1em 0; }
        a { color: #000000; }
        a:hover { text-decoration: none; }
       code { background: #F5F5F5; max-width: 100px; padding: 2px 6px; word-wrap: break-word; }
        #wrapper { background: #FFF; margin: 1em auto; max-width: 500px; width: 95%; }
        #container { padding: 2em; }
        #welcome, #status { margin-bottom: 2em; }
        #welcome h1 span { display: block; font-size: 75%; }
        #icon-status, #icon-book { float: left; height: 64px; margin-right: 1em; margin-top: -4px; width: 64px; }
        #icon-book { display: none; }

        @media (min-width: 568px) {
            #wrapper { width: 80%; margin: 2em auto; }
            #icon-book { display: inline-block; }
            #status a, #next a { display: block; }

            @-webkit-keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
            @keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
            .sf-toolbar { opacity: 0; -webkit-animation: fade-in 1s .2s forwards; animation: fade-in 1s .2s forwards;}
        }
    </style>
{% endblock %}


